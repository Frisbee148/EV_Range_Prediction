# Placeholder hyperparameters — see PRD §5.4 / §15
T_seq: 60
stride: 5
batch_size: 256
hidden_dim: 128
num_layers: 2
dropout: 0.1
use_attention: false
learning_rate: 0.001
epochs: 100
warmup_epochs: 10
lambda_max: 1.0
seeds: [0, 1, 2, 3, 4]
device: auto
